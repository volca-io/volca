import { Config } from './types/volca';

export const config: Config = {
  name: '<%= name %>',
  github: {
    organization: '<%= githubOrg %>',
    repository: '<%= githubRepo %>',
  },
  fromEmail: '<%= fromEmail %>',
  environments: {
    staging: {
      domain: '<%= environments.staging.domain %>',
      aws: {
        account: '<%= environments.staging.awsAccount %>',
        region: '<%= environments.staging.awsRegion %>',
        publicDatabase: <%= environments.staging.publicDatabase %>,
      },
    },
    production: {
      domain: '<%= environments.production.domain %>',
      aws: {
        account: '<%= environments.production.awsAccount %>',
        region: '<%= environments.production.awsRegion %>',
        publicDatabase: <%= environments.production.publicDatabase %>,
      },
    },
  },
};
